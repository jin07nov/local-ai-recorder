# PLAN

> 現在の作業のゴールと範囲。依頼内容やブランチが変わったら更新する。

## 現在の作業

- 基準日: 2026-09-05
- ブランチ: `main`
- 調査時の HEAD: `b194f30`（`Fix setup for Raspberry Pi 5 Trixie`）
- フェーズ: P0 — 管理文書と作業用コマンドの整備・検証完了。アプリ実装は未着手。

## プロジェクトのゴール

Gemma Translator を Raspberry Pi 向けのリファレンス実装として活用し、
オリジナルの機能をそのままに、会議用の文字起こし・翻訳・議事録機能を追加する。
まず whisper.cpp による長時間音声の文字起こしを完成させる。
その後、ドイツ語の選択肢を増やし、文字起こしを妨げずにドイツ語 → 日本語などをライブ翻訳してフロントエンドに表示する。
要約・決定事項・TODO・担当者・未決事項の生成は、その後の拡張として扱う。

ユーザーの構想では Pi 5、3.5 インチ LCD、USB マイク・スピーカー、Gemma、Moonshine が動作済み。
これはユーザー提示情報として扱い、今回この作業環境で実機検証したものではない。

## 今回のゴール

`ai-escape-game` を参考に作成した管理文書へ、ユーザーが整理した開発順序と既存機能維持の方針を反映する。
アプリのコードには触れず、最初の到達点と後続のライブ翻訳を区別する。
追加の依頼に従い、参考元の `/commit`・`/close`・`/issue-add` を Codex / Claude 用に移植し、
`/commit` で管理文書とスキルを現在の `main` にコミットする。

### 完了条件

- [x] `AGENTS.md` / `CLAUDE.md` に共通ルールと参照関係を定義する。
- [x] `.ai/PLAN.md` / `SPEC.md` / `TODO.md` / `KNOWLEDGE.md` を整備する。
- [x] 既存の翻訳機能と未実装の会議機能を区別する。
- [x] STT の抽象化、録音保存、議事録生成、Pi 5 での比較評価を今後の計画に落とす。
- [x] 未検証事項と判断が必要な設計を明記する。
- [x] 初期整備で文書のリンク・整合性と、新規文書6ファイルだけの変更であることを確認した。
- [x] 直近のゴールを長時間文字起こしに絞り、ドイツ語・ライブ翻訳・フロントエンド表示を次段階に置く。
- [x] 翻訳が録音・文字起こしを妨げないための要件と検証項目を記載する。
- [x] 今回の方針更新についてリンク・記述・変更範囲を再確認する。
- [x] 3コマンドと close 用テンプレートを移植し、リンク・frontmatter・両配置の同期を検証する。
- [x] `/commit` の対象を管理文書・スキル18ファイルに絞り、記録と変更範囲を確認する。コミット結果は `git log` を参照する。

### 今回の範囲外

- アプリのコード、依存ファイル、`.gitignore`、README、起動・配備設定の変更。
- whisper.cpp の導入・モデルダウンロード・録音・ベンチマーク・実機への配備。
- GitHub の fork 操作、Issue 作成、push、`/close` の実行。今回実行するコマンドは `/commit`。

## 今後の段階（実装着手前の計画）

| 段階 | ゴール | 完了の判断 |
|---|---|---|
| P0: 今回 | 文書と開発方針の整備 | 上記の完了条件を満たす |
| P1: 長時間文字起こし（直近のゴール） | whisper.cpp を導入し、長時間の会議音声を保存・文字起こし・閲覧できるようにする | 既存機能を維持し、長時間音声のタイムスタンプ付き原文を保存・表示・再生成できる |
| P2: ドイツ語・ライブ翻訳 | ドイツ語を入力言語として選べ、確定区間を日本語などへ順次翻訳して表示する | 録音中に原文と訳文が更新され、翻訳の遅延・失敗・停止によって録音・STT が止まらない |
| P3: 議事録生成 | Gemma で要約・決定事項・TODO・担当者・未決事項を保存 | 長文を分割して処理でき、発言にない担当者・期限を補わないことを評価する |
| P4: Pi での仕上げ | 小画面 UI、オフライン動作、モデルと配備方式の選定 | Pi 5 / 8GB で品質・時間・メモリ・温度・容量と既存翻訳の動作を確認する |

### P1 の完了条件（未達成）

- [ ] 既存の対応言語・2人用翻訳・キーボード操作・設定・TTS・起動方法が維持される。
- [ ] whisper.cpp で長時間音声の原文をタイムスタンプ付きで文字起こしできる。
- [ ] 音声と文字起こしを保存し、フロントエンドで結果を確認できる。失敗時に保存音声から再処理できる。
- [ ] Pi 5 / 8GB で短い音声 → 10分 → 60分の順に確認し、欠落・重複・処理時間・メモリを記録する。

60分は初回の検証目標であり、対応時間の保証ではない。P1 は録音停止後の文字起こしでもよく、
ドイツ語の UI 追加・ライブ翻訳・要約生成を完了条件に含めない。
P2 では録音中に確定区間を取り出して STT・翻訳・表示を進める。全文の完成を待ってから翻訳するだけでは P2 完了としない。
各段階の着手時に、その段階の具体的な完了条件とタスクへ PLAN / TODO を更新する。

## 後続で決めること

- 会議録音をブラウザから分割送信するか、Pi のバックエンドで直接録音するか。
- 保存先、保存容量の上限、保持・削除の操作、異常終了時の復旧範囲。
- whisper.cpp のバージョン・呼び出し方式・VAD・分割方式・モデル選択。
- Pi 5 で許容する STT 遅延と認識品質、翻訳の更新間隔、Gemma と STT の資源配分。
- ドイツ語の選択 UI と STT 経路、翻訳結果の配信方式、翻訳が追いつかない場合の表示・再処理。

これらは P0 のブロッカーではない。設計案と判断材料は [SPEC.md](SPEC.md) に置く。
