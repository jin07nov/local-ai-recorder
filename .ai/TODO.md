# TODO

> [PLAN.md](PLAN.md) のゴールをタスクに分解する。確認できたものだけ `[x]` にする。
> 現在は P1 を実装する。P2 以降は後続の Backlog。

## 進行中

- [ ] P1: 実装・ローカル検証済み。Pi でインストール → 30秒 → 10分 → 60分の E2E、認識品質・性能・既存機能の回帰を確認する。

## 完了分（P0 / 2026-09-05）

- [x] `ai-escape-game` の AGENTS / CLAUDE と `.ai` の構成・役割を確認する。
- [x] 現在の UI、録音、STT、TTS、LLM 接続、起動構成を読み取りで確認する。
- [x] `AGENTS.md` を共通ルールの正本とし、`CLAUDE.md` から参照する。
- [x] PLAN / SPEC / TODO / KNOWLEDGE をローカルAIレコーダー向けに作成する。
- [x] 既存機能・採用方針・未実装の設計案・未検証事項を分ける。
- [x] 文書間の整合性・相対リンク・UTF-8・変更範囲を確認する。新規文書6ファイルのみで、既存ファイルとインデックスの変更なし。アプリのビルド・テスト・実機検証は未実施。
- [x] 方針を「既存機能維持 → 長時間文字起こし → ドイツ語とライブ翻訳・表示 → 要約・議事録」の順に更新する。
- [x] 現在の言語一覧と、未対応言語が英語にフォールバックする STT / TTS 処理を読み取りで確認する。
- [x] 方針更新後の文書間の整合性・相対リンク・UTF-8・変更範囲を再確認する。コード・依存関係・起動設定の変更なし。
- [x] `/commit`・`/close`・`/issue-add` と close 用の3テンプレートを `.agents/skills/` / `.claude/skills/` へ移植する。
- [x] Issue の対象を local-ai-recorder に合わせ、固定 Project・共同著者を除き、close 時の後続計画の保全を定義する。
- [x] 6スキルの形式検証、両配置の6組の一致、文書18ファイルの相対リンク71件・UTF-8 を確認する。
- [x] `/commit` の手順で TODO と知見を整理し、文書・スキル18ファイルをコミット対象として確認する。アプリ実装・Issue 作成・close・push は実施していない。

## P1 の実装と検証

### P1: 長時間文字起こし（直近のゴール）

#### A. 既存機能の維持と STT の追加

- [ ] 既存の対応言語・2人用翻訳・キー操作・設定・TTS・起動方法を記録し、変更後にも同じ機能を確認する。
- [x] whisper.cpp `b4938` の CLI、多言語モデル、`.local/` の保存先を採用する。Pi 用の独立した `setup-meeting.sh` / `start-meeting.sh` を追加する。
- [ ] Pi に導入し、日本語 `base` で実際のマイク音声を認識する。
- [x] 共通 STT の WAV 入力・テキストと任意の区間結果・エラーを定義する。既存翻訳は Moonshine、会議は whisper.cpp とモードで固定する。
- [x] 最小限の STT アダプターを追加し、既存 `/api/stt` の Float32 PCM 入力と `text` 応答を自動テストする。翻訳 UI はビルド成功、実機回帰は別項目。
- [x] whisper.cpp アダプターの WAV 検証・結果解析・モデル欠落・失敗・タイムアウト・キャンセル処理を実装する。
- [x] Windows の公式 `b4938` CLI + 多言語 `base` で公開サンプル11秒を2区間に分け、認識・進捗・結果保存を確認する。Pi の性能評価とは区別する。
- [ ] 同一音声で `base` / `small` を比較し、結果を記録する。

#### B. 会議音声の保存と長時間文字起こし

- [x] ブラウザを閉じても録音を維持するため、Pi バックエンドの `arecord` と逐次 PCM 保存を採用する。
- [x] UUID・日時と時差・状態・進捗・WAV と原文の保存・容量下限・録音時間上限・確認付き削除を定義する。
- [x] 音声・原文・メタデータ・モデル・設定を含む `.local/` 全体を Git 追跡対象外にする。
- [x] 録音開始・停止・逐次保存・WAV 確定と、保存済み音声から再処理するジョブを実装する。
- [x] 分割結果の保存・再開・モデル変更時の再生成と、全体時刻・重複しない区間 ID を自動テストする。
- [ ] 分割処理・VAD を評価し、発話の欠落・重複と全体タイムスタンプを確認する。
- [x] 録音状態・経過時間・処理進捗・結果・エラー・ダウンロードを会議 UI に追加し、既存画面とのビルドを確認する。
- [ ] Pi の3.5インチ画面で録音・停止・閲覧・再開・削除を実操作する。作業環境のブラウザは利用不可だった。
- [x] 自動テスト21件で、HTTP の録音→停止→文字起こし→取得→削除、切断・中断・上限・容量不足・復旧・再起動後の区間再利用・二重起動・不正 WAV を確認する。音声機器と認識モデルはテスト用に差し替えた。
- [x] Bash 構文を確認し、Pi 導入・設定・モデル比較・実機 E2E の手順を `docs/meeting-recorder.md` に作成する。実機確認が必要になったことをユーザーへ案内する。
- [x] P1 実装 `47304e4` を競合なく `main` へ取り込み、Pi 実機 E2E 待ちの記録を維持する。
- [ ] 10分 / 60分で保存・文字起こしを確認し、容量不足・中断・再実行を検証する。
- [ ] 翻訳・要約を有効にせず P1 の完了条件を満たすことを確認する。

## Backlog（将来の実装）

### P2: ドイツ語の追加とライブ翻訳・表示（P1 の次）

- [ ] ドイツ語（`de`）を入力言語に追加し、日本語（`ja`）への翻訳先指定を UI から処理経路まで結ぶ。
- [ ] モード・STT・翻訳・TTS の対応言語を整理し、ドイツ語を英語へフォールバックさせず whisper.cpp へ渡す。
- [ ] ドイツ語の基準音声を用意し、原文の認識と Gemma による日本語訳の品質を個別に確認する。
- [ ] 録音中の確定音声区間を順次 STT へ渡し、原文区間の ID・時刻・言語を保存・表示する。
- [ ] 原文確定後に独立した翻訳キューへ登録する。翻訳待ちが録音・STT をブロックしない処理にする。
- [ ] 翻訳の同時実行数・メモリ上限・CPU/メモリ配分と、負荷時の遅延・一時停止・後続再開を設計する。
- [ ] フロントエンドへ原文と訳文を順次配信し、区間の対応・順序・再接続時の重複と取得漏れを確認する。
- [ ] 翻訳オン/オフ、翻訳待ち・遅延・一時停止・失敗を表示する。録音・STT の状態とは分ける。
- [ ] 翻訳の遅延・タイムアウト・Gemma 停止を再現し、録音・文字起こし・原文表示が続くことを確認する。
- [ ] Pi 5 で翻訳オフ/オンを比較し、STT の欠落・重複・遅延・滞留と資源使用量を評価する。
- [ ] ドイツ語で会議録音を続けながら日本語訳が更新され、既存 Translator Mode も従来どおり動作することを確認する。

### P3: ローカル議事録

- [ ] Gemma の入力長とメモリ予算を確認し、分割要約・全体統合を設計する。
- [ ] 3行要約・決定事項・TODO・担当者・期限・未決事項の形式とプロンプトを定義する。
- [ ] 文字起こしを維持して `summary.md` を保存・再生成できるようにする。
- [ ] 根拠のない決定・担当者・期限が追加されないか、無音や曖昧な発言も含めて評価する。

### P4: Raspberry Pi での仕上げ

- [ ] Pi 5 / 8GB で処理時間・メモリ・温度・容量を測り、モデルと常駐方針を決める。
- [ ] 必要な STT / TTS / LLM モデルを事前配置し、ネットワークを切った状態で一連の処理を確認する。
- [ ] 3.5 インチ LCD と USB 音声機器で操作し、両モードの切り替えと既存翻訳の回帰を確認する。
- [ ] 保存権限・起動・停止・再起動時の扱いを踏まえ、必要な配備設定を更新する。
- [ ] README を実装済みの機能・導入手順・計測結果に合わせて更新する。

## P1 の対象外（後続で扱う）

- ドイツ語の UI 追加・ライブ翻訳・逐次表示は P2、要約・議事録の生成は P3。
- P1 は録音停止後の文字起こしを許容する。録音中の区間処理は P2 で追加する。

## 当面の対象外

- 話者分離・実名特定、新しい会議機能での訳文の音声読み上げ、単語単位の即時字幕。
- クラウド同期、外部 SaaS 連携、複数会議の並列処理。
- UI の全面刷新、筐体の変更、Moonshine の削除、大型 Whisper モデルの初回採用。
