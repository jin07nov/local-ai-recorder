# AGENTS.md - local-ai-recorder

## プロジェクト概要

Gemma Translator を土台に、Raspberry Pi 向けの「スノークAIラボのローカルAIレコーダー」を作る。
オリジナルの機能・操作をそのまま維持し、会議の録音・文字起こし・翻訳・議事録機能を追加する。

- **Translator Mode**: Moonshine STT → Gemma / LiteRT-LM → TTS。
- **直近の開発ゴール（実装済み・Pi 実機検証待ち）**: whisper.cpp の長時間文字起こしと、追加依頼による録音中の原文の逐次表示。Pi の録音成功はユーザー報告あり。長時間性能とライブ表示は実機検証待ち。翻訳・要約の完成を条件にしない。
- **その次のゴール（未実装）**: ドイツ語の選択肢を追加し、ドイツ語 → 日本語などのライブ翻訳をフロントエンドに表示する。録音・文字起こしを優先する。
- **後続の議事録機能（未実装）**: 文字起こし → Gemma → 要約・決定事項・TODO。
- 主な対象は Raspberry Pi 5 / 8GB RAM、3.5 インチ LCD、USB マイク・スピーカー。
- セットアップ後の録音・推論・保存が端末内で完結することを目指す。

## 作業の進め方

プロジェクトの計画・管理は `.ai/` で行う。作業開始時に次の4ファイルを読む。

| ファイル | 役割 |
|---|---|
| [.ai/PLAN.md](.ai/PLAN.md) | 現在の作業のゴール・対象範囲・完了条件、今後の段階 |
| [.ai/SPEC.md](.ai/SPEC.md) | 現状の構成、採用方針、設計案、未確定事項 |
| [.ai/TODO.md](.ai/TODO.md) | 現在のタスクと将来の Backlog。完了したものだけ `[x]` にする |
| [.ai/KNOWLEDGE.md](.ai/KNOWLEDGE.md) | 調査で確認した注意点、実際につまずいた原因と対処 |

1. ユーザーの最新の依頼と `PLAN.md` から今回の範囲を確認する。
2. 実装済み・採用方針・設計案・未検証を区別する。提案やユーザー報告を実測結果に置き換えない。
3. 方針が変わったら PLAN / SPEC、進捗が変わったら TODO、再利用できる知見は KNOWLEDGE に反映する。
4. 説明と管理文書は日本語を基本とし、コード上の識別子・コマンド・エラー原文は維持する。
5. 完了報告には変更したもの、確認したこと、未確認のことを簡潔に記載する。

**現在は P1 への追加依頼として、録音中の文字起こしと原文の逐次表示が対象。ドイツ語・ライブ翻訳は後続に残す。**
将来の Backlog があることを実装着手の指示と解釈しない。
ユーザーから実装の依頼があったら、その範囲に合わせて PLAN / TODO を更新して進める。

## 作業用コマンド

参考元から移植した3つのスキルを使う。`/commit` などの依頼を受けたら、対応する SKILL.md を読んで実行する。

| コマンド | 役割 | Codex 用の定義 |
|---|---|---|
| `/commit` | TODO と必要な知見を更新し、対象の変更をコミットする | [.agents/skills/commit/SKILL.md](.agents/skills/commit/SKILL.md) |
| `/close` | 完了フェーズの記録を整理し、後続計画・知見を保全する | [.agents/skills/close/SKILL.md](.agents/skills/close/SKILL.md) |
| `/issue-add` | 指定された作業をこのリポジトリの GitHub Issue にする | [.agents/skills/issue-add/SKILL.md](.agents/skills/issue-add/SKILL.md) |

Claude 用は `.claude/skills/` の同名ディレクトリに配置する。更新時は両方の定義・テンプレートを同期する。
Codex のスキル読み込み先は `.agents/skills/`。明示的なスキル指定は `$commit` などでも行える。
参照: [公式スキルドキュメント](https://learn.chatgpt.com/docs/build-skills)。
コマンドの移植や `/commit` を、`/close`・Issue 作成・push の実行指示に広げない。

## 設計の基本方針

- 既存の言語・2人用翻訳・キー操作・設定・TTS・起動方法を維持する。Moonshine を削除せず、STT の分離は追加機能に必要な範囲に留める。
- 録音保存・STT・翻訳・要約を分離する。原文の保存と表示は翻訳を待たず、翻訳の遅延・失敗・停止が録音と文字起こしを止めない設計にする。
- ライブ翻訳は確定した文字起こしの区間から順次処理する。CPU・メモリの競合時は文字起こしを優先し、翻訳を遅延・一時停止できるようにする。
- 言語の追加は UI と処理経路の両方で扱う。ドイツ語を未対応の既存 STT に渡して英語へフォールバックさせない。
- Pi 5 のメモリ・処理時間・発熱・保存容量を実機で確認する。リアルタイム動作やモデルの優劣を断定しない。
- 原文の文字起こしを翻訳・要約で上書きしない。担当者・期限・決定事項は発言に根拠がなければ未指定・未確認とする。
- 音声・文字起こし・議事録を外部サービスへ自動送信しない。実データやモデルファイルを Git に含めない。
- 上流由来のライセンス・著作権表記を維持し、用途と無関係なリファクタリングや改名を混ぜない。

## リポジトリの読み方と確認

- `frontend/`: React / Vite の UI とブラウザ録音。
- `backend/server.py`: Python 標準ライブラリの HTTP サーバー。STT・TTS・ローカル LLM プロキシ。
- `backend/meeting_server.py` / `meetings.py` / `stt.py`: 独立した会議 API・録音と復旧・STT アダプター。
- `frontend/src/meeting/` / `frontend/meeting.html`: 会議用 UI。既存翻訳 UI と同時にビルドする。
- `setup-meeting.sh` / `start-meeting.sh`: 会議専用の導入と起動。手順と E2E は [docs/meeting-recorder.md](docs/meeting-recorder.md)。
- `start.sh` / `setup.sh` / `download_model.sh` / `deploy-pi.sh` / `deploy/`: 起動・導入・Pi 配備。
- [README.md](README.md): 会議用の導入案内と、上流の Gemma Translator の説明。
- バージョンは依存定義・ロックファイルを確認する。存在しないテストや起動コマンドを記載しない。
- 文書だけの変更ではリンク・記述の整合性・差分範囲を確認する。アプリのビルドや実機確認を済ませたことにしない。
- 自動テストは `python -m unittest discover -s tests -v`、UI ビルドは `npm --prefix frontend run build`。録音・推論・配備の確認は対象の Linux / Pi 環境で別途行う。

## ブランチ運用

- ブランチを指定されたら、そのブランチを使う。途中で作業先を変えない。
- 指定がなければ現在のブランチと未コミット変更を確認し、作業用ブランチが必要なら内容に合ったものを1本使う。
- 現在の作業ブランチは PLAN に記録する。ユーザーの変更を巻き戻さない。
- コミット・プッシュ・Issue 作成・配備は、その時点のユーザーの依頼範囲に従う。文書内の将来計画を実行指示とみなさない。

共通ルールはこのファイルを正本とし、[CLAUDE.md](CLAUDE.md) に重複管理しない。
